name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements-dev.txt
      - run: ruff check app tests
      - run: ruff format --check app tests
      - run: pytest -q

  website:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: website
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: website/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build
        env:
          NEXT_PUBLIC_LEXGUARD_API_URL: https://lexguard-api-3mvnlmbu5q-el.a.run.app
          NEXT_PUBLIC_FIREBASE_API_KEY: AIzaSyCiQjE72J2zXzPtPuTld9jBCudD9EZ0AvM
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: lexguard-manish-75853.firebaseapp.com
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: lexguard-manish-75853
          NEXT_PUBLIC_FIREBASE_APP_ID: 1:311207483497:web:9db59beecf7c86df282af8
